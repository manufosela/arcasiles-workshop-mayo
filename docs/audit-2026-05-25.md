# kj-audit · Informe de auditoría del repo `clubes-lectura`

> **Fecha**: 2026-05-25
> **Branch auditado**: `main` @ `7782daa fix(clubes-lectura): repara renderizado, mapa, membership y enriquece datos (#2)`
> **Stack**: Astro 5.18.1 · Lit 3.2.1 · Tailwind v4 (beta) · TypeScript strict · Leaflet 1.9.4
> **Lifecycle**: demo del workshop ai-engineering Reto B; spec-001 SHIPPED · D-001-08 excluye tests exhaustivos del scope
> **PRs relacionadas**:
> - [#1 — feat: app de clubes de lectura](https://github.com/manufosela/arcasiles-workshop-mayo/pull/1)
> - [#2 — fix: repara render, mapa, membership y enriquece datos](https://github.com/manufosela/arcasiles-workshop-mayo/pull/2)

---

## 1. Security — **B+**

| Sev | Fichero · Línea | Regla | Hallazgo | Recomendación |
|-----|------------------|-------|----------|---------------|
| 🟡 Med | `dependencies` | CVE | 2× moderate CVE en Astro 5 (`define:vars` XSS, server-island replay) + 1 transitiva en `yaml`. `npm audit --audit-level=high` exit 0 (ninguna high/critical). | `npm audit fix --force` → upgrade a `astro@6` (breaking). Diferible: cero impacto en este demo SSG sin server islands. |
| 🟢 Low | `src/components/clubs-map.lit.ts:133-134` | XSS-Pattern | `bindPopup` usa template literal con `${c.name}`, `${c.city}`, `${c.neighborhood}` interpolados como HTML crudo. **Datos de JSON estático seed → riesgo real = 0**. Pero el patrón es contagioso. | Usar `DOMPurify` o construir el popup con DOM API (`document.createElement` + `textContent`). Documentar como práctica defensiva. |
| 🟢 Low | `src/components/clubs-map.lit.ts:75` | TS-Suppression | 1 `@ts-expect-error` justificado para parche de iconos Leaflet (API undocumented). | Aceptable. Está acotado y tiene comentario explicativo. |
| ✅ | toda la app | Secrets | `gitleaks detect` clean (verificado en `/ai-verify` anterior). Cero `api_key`, `password`, `token`, `process.env` hardcoded. | — |
| ✅ | `src/components/club-forum.lit.ts:148` | XSS-Foro | `<p class="body">${m.body}</p>` — Lit `` html`` `` escapa strings automáticamente. Posteo seguro. | — |
| ✅ | toda la app | Auth | No hay auth (per D-001-08). N/A en este scope. | — |

---

## 2. Code Quality — **A−**

| Sev | Fichero · Línea | Regla | Hallazgo | Recomendación |
|-----|------------------|-------|----------|---------------|
| 🟡 Med | `src/components/filter-bar.lit.ts:137` | LongFunction | `render()` 76 LOC (mayoría es template HTML inline + listeners). | Extraer `renderSelect()`, `renderActions()` helpers. Mejora legibilidad sin cambiar lógica. |
| 🟡 Med | `src/components/profile-form.lit.ts:134` | LongFunction | `render()` 67 LOC, mismo patrón. | Mismo refactor. |
| 🟢 Low | `src/components/clubs-map.lit.ts:51` | LongFunction | `initMap()` 51 LOC. Es secuencial: cargar L → CSS → iconos → mapa → markers. | Aceptable, secuencia clara con comentarios. |
| 🟢 Low | `src/pages/index.astro:65` | Console-In-Prod | 1 `console.error('clubs-data element missing')` en una rama imposible. | Sustituir por `throw` o quitar (`dataEl!` ya asume su existencia). |
| 🟢 Low | varios `.lit.ts` | Type-As-Any | `mapEl as any` en `index.astro`, `private map?: any` en `clubs-map`. Necesario porque los custom elements se tipan sin interfaces compartidas exportadas a las pages. | Crear `src/types/components.ts` con declaraciones de los custom elements o exponer interfaces públicas tipadas. |
| ✅ | LOC ranking | FileSize | Max 220 LOC (filter-bar). Promedio 130. Bajo el umbral de 500. | — |
| ✅ | toda la app | Suppression | Cero `@ts-ignore`, cero `eslint-disable`, cero `NOSONAR`. Solo 1 `@ts-expect-error` justificado. | — |
| ✅ | toda la app | TODO/FIXME | Cero TODOs, FIXMEs, HACKs. | — |
| ✅ | error handling | Try-Catch | Lit components manejan estado de error UI (botón `disabled`, badges `none/pending/accepted/rejected`). Repos usan `try/catch` en JSON parsing. | — |

---

## 3. Performance — **B**

| Sev | Fichero · Línea | Regla | Hallazgo | Recomendación |
|-----|------------------|-------|----------|---------------|
| 🟡 Med | `src/pages/index.astro` | Eager-Load | Leaflet (150 KB minified) se importa en el script de la home aunque el usuario nunca abra la vista Mapa. | Lazy-import dinámico cuando se toggle a Mapa: `await import('../components/clubs-map.lit.ts')`. Ahorra ~150 KB en first paint. |
| 🟡 Med | `src/pages/index.astro:80-95` | FullRerender | Cada cambio de filtro hace `results.innerHTML = ''` y recrea N `<club-card>`. Para 20 clubs es OK; para >100 sería costoso. | Si crece la cardinalidad, usar `lit-html` repeat directive en lugar de DOM imperativo. |
| 🟢 Low | `dist/_astro/leaflet-src.*.js` | BundleSize | 150 KB para Leaflet. Inevitable dado su uso. | — |
| 🟢 Low | `src/services/booksRepo.ts:hash` | Hash-Loop | `hash()` itera string char por char en cada `averageRating()` call. 30+ libros × varios calls por render. | Memorizar con `Map<string, number>`. Microoptimización. |
| ✅ | bundle dist/ | Total | 572 KB total (incluyendo Leaflet + Lit + 22 páginas HTML). | — |
| ✅ | data layer | N+1 | Datos JSON estáticos cargados como módulo. Cero requests por entidad. | — |
| ✅ | Tailwind v4 | TreeShake | Tree-shaking automático del CSS no usado. | — |

---

## 4. Architecture — **A**

| Sev | Fichero · Línea | Regla | Hallazgo | Recomendación |
|-----|------------------|-------|----------|---------------|
| ✅ | hexagonal gate | LayerSep | **Cero violaciones**: ningún componente o page importa de `src/data/*.json`. Solo los 6 ficheros en `src/services/*Repo.ts` lo hacen (correcto: son los adapters del puerto). | — |
| ✅ | `src/types/` | SharedTypes | 7 ficheros de tipos compartidos con barrel `index.ts`. Tipos minimalistas, sin duplicación. | — |
| 🟢 Low | `src/styles/globals.css` | TokenDuplication | Tokens definidos dos veces: `--color-paper` (Tailwind v4 `@theme`) y `--paper` (`:root` para shadow DOM). Necesario porque Tailwind no atraviesa shadow, pero crea fuente doble. | Documentar el trade-off con comentario en `globals.css`. Considerar un script de sincronización si crece. |
| 🟢 Low | `src/components/` | NoBarrel | No hay `src/components/index.ts` ni `src/services/index.ts`. Cada import es relativo individual. | Añadir barrels para DX más limpia. |
| 🟢 Low | `src/pages/index.astro:46` | Workaround | Datos pasados via `<div data-clubs={JSON.stringify(...)}>` en lugar de `define:vars`. Justificado por limitación Astro+Vite (PR#2). | Está documentado en el commit. OK. |
| ✅ | dependencias | Circular | Cero dependencias circulares detectadas (services → types, components → services + types, pages → components + services + types). | — |
| ✅ | docs | Architecture | README + spec.md + plan.md (historial en archive) documentan la decisión Hexagonal + Repository. | — |

---

## 5. Testing — **F (per spec)**

| Sev | Fichero · Línea | Regla | Hallazgo | Recomendación |
|-----|------------------|-------|----------|---------------|
| ⚪ N/A | N/A | TestCoverage | **0 tests** en el repo. Sin Vitest, sin Playwright, sin coverage report. | **Decisión D-001-08** lo excluye explícitamente del scope del workshop. Para producción: añadir smoke tests al menos sobre los 6 repos (puro JSON in → datos out, fácil de testear). |
| ⚪ N/A | N/A | E2E | Cero tests E2E. La verificación se hizo manualmente con Chrome DevTools MCP. | Añadir un Playwright test del flujo de demo del workshop (`filtro Madrid + lit. latinoamericana → click → ver detalle`). |
| ⚪ N/A | N/A | CI | Solo GitGuardian Security en PRs. Sin pipeline de tests. | Si se mantiene el repo: añadir GitHub Action que corra `astro check` + `npm run build` en cada PR. |

---

## Overall Health — **good (para demo) / fair (si fuera producción)**

| Dimensión | Score |
|-----------|-------|
| Security | **B+** — 2 CVE moderate en Astro 5 (no bloqueantes), 1 XSS pattern teórico en `bindPopup` mitigado por origen confiable de datos |
| Code Quality | **A−** — cero supresiones, ficheros pequeños, error handling presente; 2 `render()` largos refactorizables |
| Performance | **B** — Leaflet eager-loaded (150 KB), todo lo demás OK para la escala actual (20 clubs) |
| Architecture | **A** — hexagonal gate intacto, tipos compartidos, sin circular deps |
| Testing | **F (per spec)** — D-001-08 lo excluye del scope; aceptable para demo, no para producción |

---

## Top Recommendations (prioritized)

| # | Acción | Impacto | Effort | Cuándo |
|---|--------|---------|--------|--------|
| 1 | **Lazy-load Leaflet** al toggle a Mapa | **Alto** (−150 KB en first paint, ~250 ms en 3G) | 15 min | Si el repo sigue vivo post-workshop |
| 2 | **Sanitizar `bindPopup`** con DOM API (textContent) en `clubs-map.lit.ts:133` | **Medio** (defensa en profundidad, patrón contagioso) | 10 min | Antes de aceptar PRs que añadan datos de origen externo |
| 3 | **Refactor `render()`** de filter-bar y profile-form a sub-functions | **Bajo** (legibilidad) | 20 min | Cleanup oportunista |
| 4 | **Tipar custom elements** en `src/types/components.ts` para eliminar `as any` en pages | **Medio** (TypeScript strict de verdad) | 30 min | Solo si decides mantener TS |
| 5 | **Smoke tests de repositorios** (Vitest sobre `clubsRepo.filter`, `userRepo`, `membershipRepo`) | **Alto** si va a producción | 1-2 h | Cuando salga del scope "demo" |
| 6 | **Astro 5 → 6** + `npm audit fix --force` | **Alto** para CVEs, **breaking** | 2-3 h | Cuando se decida vida útil real |
| 7 | **GitHub Action**: `astro check` + `npm run build` en PRs | **Alto** para mantenimiento | 30 min | Si hay siguiente PR |
| 8 | **Memoizar `booksRepo.averageRating`** con `Map` | **Muy bajo** (microopt) | 5 min | Si los ratings se llaman cientos de veces |

---

## Evidencia recolectada (resumen de comandos ejecutados)

```
git log --oneline -3
  → 7782daa fix(clubes-lectura): ... (#2)
  → d0cdc82 feat(reto-b): app de clubes de lectura (#1)
  → 553f08d chore: initial sandbox

LOC ranking
  → 220 src/components/filter-bar.lit.ts
  → 208 src/components/profile-form.lit.ts
  → 170 src/components/club-forum.lit.ts
  → 159 src/components/clubs-map.lit.ts
  →  ...
  → 1962 total LOC en src/

grep -rn 'innerHTML|@ts-ignore|TODO|FIXME|api_key|password|token' src/
  → 1× @ts-expect-error (justificado, Leaflet)
  → 1× console.error en rama imposible
  → 0× resto

grep -rn 'from.*data/' src/components/ src/pages/ src/layouts/
  → (vacío — hexagonal gate intacto)

npm audit --omit dev --audit-level=high
  → exit 0 (6 moderate, 0 high/critical)

du -sh dist
  → 572K (incluyendo Leaflet 150K)
```

---

## TL;DR

Para una demo de workshop de 60 min, la calidad es **muy alta**: arquitectura limpia (hexagonal + repository), cero deuda técnica visible (no TODOs/FIXMEs/suppressions), gates de governance verdes (gitleaks, `astro check`, build). Los flacos están donde el spec los excluyó explícitamente (tests, deploy) o donde el upgrade es cosmético (Leaflet lazy-load, Astro 6).

Si planeas convertirlo en producto real: las prioridades 1, 5, 6 y 7 son las que mueven la aguja.
